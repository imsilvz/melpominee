apiVersion: v1
kind: ServiceAccount
metadata:
  name: melpominee
  namespace: melpominee
  annotations:
    # Client ID of melpominee-app-identity (managed by Terraform in imsilvz/azure-aks).
    # Must match the clientID in secrets/melpominee.yaml (SecretProviderClass) in that repo.
    azure.workload.identity/client-id: "6b144275-5d8a-4eeb-bbd0-00cabab0a2e8"
  labels:
    azure.workload.identity/use: "true"
